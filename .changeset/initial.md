---
"sprung": minor
---

Initial public release: closed-form damped-harmonic-oscillator solver (`createSpring`), velocity-continuous interruptible controller (`spring`), the `fromFeel` designer mapping, named `presets`, and the `useSpring` React hook (`sprung/react`). Zero-dependency, SSR-safe, dual ESM+CJS.
